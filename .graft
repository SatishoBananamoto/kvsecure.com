# NEXT SESSION - START HERE

## Current State

The static kvsecure.com site was rewritten into a trust-first launch surface:

- `index.html` now leads with the local vault and brokered credential boundary.
- `styles.css` is the shared visual system for the site.
- `site.js` handles mobile nav, FAQ accordion, copy button, reveal states, and the hero flow event loop.
- `guide.html`, `faq.html`, and `feedback.html` were refreshed to remove stale launch claims and match the homepage story.
- `tools/check_site.py` and the `Site Checks` GitHub Actions workflow guard the static launch pages against broken local links and overbroad security/availability claims.
- `LICENSE` now gives the public site repo a detected MIT license.
- The hero demo now has a manual switch for `Normal path` and `With kv`, showing both the weak raw-key path and the kv-internal broker path.
- The hero demo animation was changed from absolute overlay ribbons/tokens to contained lane tracks so moving labels cannot overlap cards or boundary notes.
- The hero demo animation was reworked again from an image-generated concept into a broker-chamber visual with contained beams, a central kv boundary, and separate normal/kv states.
- A live browser QA pass found and fixed two site issues:
  - `site.js` now catches clipboard write rejection, tries a legacy copy fallback, and shows `Copy unavailable` instead of throwing an unhandled promise.
  - `styles.css` now overrides compact feedback-card heading spacing so labels like `Report a bug` and `Request a feature` keep visible word separation.

## Product Story To Preserve

Use this framing:

- kv helps users store secrets locally and let AI agents use them without handing over raw credentials by default.
- Brokered tools (`kv_api`, `kv_query`, `kv_ssh`) are the safer path.
- `kv run` is useful but higher risk because selected secrets can enter a subprocess environment.
- Team/cloud sync is future or private-beta language only until production evidence exists.

Avoid these claims:

- "military-grade encryption"
- fixed team pricing
- old versions or old test counts
- "agent never sees secrets" as an absolute product-wide promise
- team/cloud sync as a live public feature

## Verification From This Pass

- `node --check site.js`
- Python HTML parser over `index.html`, `guide.html`, `faq.html`, and `feedback.html`
- local asset reference check for `styles.css` and `site.js`
- stale-claim search for old version, fake pricing, old test count, "military-grade", and absolute never-see wording
- `git diff --check`
- `/home/satishocoin/kv-secrets/scripts/exit_gate.sh /home/satishocoin/kvsecure.com`
- After the hero demo rework: `node --check site.js`, Python HTML parser, local link check, stale-claim search, non-ASCII scan, `git diff --check`, and local HTTP asset checks for `/`, `styles.css`, and `site.js`.
- After fixing animation overlap: screenshot-style renders were generated for desktop `With kv` and `Normal path` modes at `/tmp/kvsecure-kv-current.png` and `/tmp/kvsecure-normal-current.png`.
- After the broker-chamber rework: generated concept image at `/home/satishocoin/.codex/generated_images/019dada6-8635-7292-9e83-a7139de88e6d/ig_0a877fc0f555025f0169f912a0fae4819194d563df602d8bbf.png`; screenshot-style renders at `/tmp/kvsecure-kv-stage-beams.png` and `/tmp/kvsecure-normal-stage-beams2.png`.
- 2026-05-11 live QA after commit `1f34bc8`: GitHub Pages source is `main` at `/`, latest Pages build for `1f34bc8e3fff4de7ef8b6209eb0e41666c782eb2` reported `built`; `https://kvsecure.com/?v=1f34bc8` loaded in headless Chromium with no console issues, no network failures, no 4xx/5xx asset responses, no framework overlay, and no horizontal overflow at desktop or mobile widths. Screenshots were captured under `/tmp/codex-kvsecure-live-qa/`.
- Focused live interaction smoke after `1f34bc8`: hero toggles switched to `Normal path` and `With kv`; denied clipboard write showed `Copy unavailable` with no unhandled rejection; FAQ accordion opened with `aria-expanded="true"` and visible answer; mobile nav opened; mobile `Normal path` mode activated with zero horizontal overflow.
- 2026-07-03 public-readiness audit: `node --check site.js`, HTML parser over all four pages, secret-pattern scan, claim scan, source-claim spot checks against `/home/satishocoin/kv-secrets`, and local Chromium CDP QA at desktop/mobile widths passed. The local QA exercised both hero demo modes, clipboard rejection fallback, FAQ accordion, mobile nav, feedback form required fields, and horizontal overflow checks. Screenshots and JSON result were written under `/tmp/codex-kvsecure-qa/`.

## Open Next Step

The GitHub Pages browser review is complete for commit `1f34bc8`. For future
site edits, rerun the same live QA path after Pages builds:

1. Open the homepage on desktop and mobile widths.
2. Check both hero demo modes: `Normal path` and `With kv`.
3. Check nav, copy button, FAQ accordion, and feedback form layout.
4. If CDN cache shows stale assets, reload with a cache-busted URL such as `https://kvsecure.com/?v=<commit>`.
